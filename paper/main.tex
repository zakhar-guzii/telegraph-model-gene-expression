\documentclass[11pt]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{caption}
\usepackage{placeins}
\usepackage{authblk}
\usepackage{xcolor}
\usepackage[colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue]{hyperref}

\graphicspath{{figures/}}
 
% Convenient shorthands for the moments
\newcommand{\muG}{\mu_G}
\newcommand{\muR}{\mu_R}
\newcommand{\sigGsq}{\sigma_G^2}
\newcommand{\sigRsq}{\sigma_R^2}
\newcommand{\CRG}{C_{RG}}
\newcommand{\kon}{k_{\mathrm{on}}}
\newcommand{\koff}{k_{\mathrm{off}}}
\newcommand{\ksyn}{k_{\mathrm{syn}}}
\newcommand{\kdeg}{k_{\mathrm{deg}}}
\newcommand{\E}{\mathbb{E}}

\title{\textbf{The Telegraph Model of Gene Expression:\\
A Triangular Cross-Validation of Stochastic Simulation}}

\author{Zakhar Guzii}
\affil{Kyiv School of Economics, Department of Mathematics\\
\texttt{zakharguzii@gmail.com}}
\date{July 2026}

\begin{document}

\maketitle

\begin{abstract}
Gene expression is a stochastic process: even genetically identical cells in the same
environment produce very different numbers of messenger RNA (mRNA) molecules. The
telegraph model explains the main source of this variability. It treats a gene as a
two-state (ON/OFF) switch that transcribes mRNA only while active. We study the
telegraph model through three algebraically equivalent descriptions, all following from
the same chemical master equation, and check that their independent implementations
agree: we (i) generate stochastic trajectories with the Gillespie
stochastic simulation algorithm (SSA); (ii) derive and solve a closed system of
ordinary differential equations (ODEs) for the first and second moments; and
(iii) draw independent samples directly from the analytic steady-state
distribution, using the classic Beta--Poisson law together with an explicit
Bernoulli gene state. The main
contribution is the implementation cross-validation that links the three. The code is available at
\url{https://github.com/zakhar-guzii/telegraph-model-gene-expression}.
\end{abstract}

\section{Introduction}

Gene expression is variable.
Two cells with the same DNA, grown together in the same environment, can have different numbers of copies of the same mRNA or protein~\cite{elowitz2002,ozbudak2002}.
This difference between cells is called phenotypic noise. It happens naturally because of random molecular interactions. Experimentally this variability separates into two parts~\cite{elowitz2002}: \emph{extrinsic} noise, 
caused by differences between cells---their size, cell-cycle stage, or the pool of available polymerases---and \emph{intrinsic} noise, 
the randomness of the transcription and degradation events themselves, which persists even between two identical copies of a gene inside the same cell.

Inside a cell the molecules involved---a single copy of the gene, a few transcription factors,
and a few dozen mRNA molecules---are present in such small numbers that the discreteness and randomness of individual chemical events cannot be smoothed out.
The telegraph model studied here is the simplest framework that describes this intrinsic component.
It treats the promoter as a switch that moves at random between an active (ON) and an inactive (OFF) state~\cite{ko1991}.
While ON, it produces mRNA molecules through transcription in random bursts; each molecule is then degraded at a random time. As a result, the mRNA count is a discrete-valued stochastic process.

The telegraph model reproduces the bursty, \emph{over-dispersed} mRNA distributions seen in single-molecule experiments,
which count individual mRNAs inside single cells~\cite{raj2006}, and it can be analysed exactly~\cite{peccoud1995}. 
A convenient way to measure this spread is the \emph{Fano factor} $F=\sigma^2/\mu$~\cite{fano1947}, the ratio of the variance to the mean of the mRNA count. 
Pure Poisson production and decay, in which every event is independent, has variance equal to mean, giving the \emph{Poisson baseline} $F=1$. 
``Over-dispersed'' means $F>1$: the random switching of the gene adds extra variability on top of that baseline.

We treat the telegraph model as a case study in how a stochastic process can be described from several mathematically distinct viewpoints. 
We implement three approaches that look unrelated:

\begin{enumerate}
  \item a stochastic simulation (the SSA), which follows a single cell through time;
  \item a system of moment ODEs, which gives the population mean and spread
    deterministically;
  \item a steady-state sampler, which describes the population after a long time.
\end{enumerate}

The three are algebraically equivalent---all follow from the same chemical master equation---but
their implementations share no code and fail in different ways. Agreement between them is therefore
evidence against implementation defects, not evidence about the master equation they all inherit.

\section{Model Formulation}
\label{sec:model}

The state of the system at time $t$ is the pair
\begin{equation}
  \big(G(t),\,R(t)\big), \qquad G(t)\in\{0,1\}, \quad R(t)\in\mathbb{N},
\end{equation}
where the binary variable $G$ records whether the gene is OFF ($G=0$) or ON
($G=1$), and the integer $R$ counts the number of mRNA molecules present.
 Four basic reactions define the dynamics of the system. 
 Each reaction has a \emph{propensity} $a_j$, the rate at which it fires at any given moment:

\begin{center}
\begin{tabular}{llll}
\toprule
\textbf{Reaction} & \textbf{Description} & \textbf{Effect} & \textbf{Propensity} \\
\midrule
$R_1$ & Gene activation   & $G:0\to1$       & $a_1 = \kon\,(1-G)$ \\
$R_2$ & Gene inactivation & $G:1\to0$       & $a_2 = \koff\,G$ \\
$R_3$ & RNA synthesis     & $R\to R+1$      & $a_3 = \ksyn\,G$ \\
$R_4$ & RNA degradation   & $R\to R-1$      & $a_4 = \kdeg\,R$ \\
\bottomrule
\end{tabular}
\end{center}

All four rate constants are positive. The switching rates $\kon,\koff$ set how quickly
the promoter turns on and off, and their ratio fixes the long-run fraction of time the
gene is active; the factor $G$ in $a_3$ shuts transcription off entirely when the gene
is OFF; and $\kdeg$ is a per-molecule decay rate, so the total degradation propensity
$\kdeg R$ grows with the amount of mRNA present. Together the rates
$(\kon,\koff,\ksyn,\kdeg)$ and an initial condition $(g_0,r_0)$ at time $t_0$ fully
determine the law of the process.

\paragraph{Burst size.}
Once the gene is ON, the only
reaction that can switch it off fires at the constant rate $\koff$, so the duration of an active
period is exponential with mean $1/\koff$; during that period transcription fires at rate $\ksyn$.
The mean number of transcripts made per active period---the \emph{burst size} $B$---is therefore
\begin{equation}
\label{eq:burst}
  B = \ksyn\,\E[\text{ON duration}] = \frac{\ksyn}{\koff}.
\end{equation}
Bursts of this kind are observed directly in single cells~\cite{golding2005,raj2006}, and the exact
consequences of promoter switching for the mRNA distribution, including the limits recovered in
Section~\ref{sec:res-explore}, are derived in~\cite{iyerbiswas2009}.

Because each waiting time is exponential and depends only on the current state,
$(G(t),R(t))$ is a continuous-time Markov chain (CTMC). Its
distribution $P_t(g,r)=\Pr\{G(t)=g,\,R(t)=r\}$ evolves according to the chemical
master equation~\cite{vankampen}
\begin{equation}
\label{eq:master}
\begin{aligned}
  \frac{\mathrm{d}}{\mathrm{d}t} P_t(g,r)
  ={}& \kon\big[\delta_{g,1}P_t(0,r) - \delta_{g,0}P_t(g,r)\big] \\
   &+ \koff\big[\delta_{g,0}P_t(1,r) - \delta_{g,1}P_t(g,r)\big] \\
   &+ \ksyn\,\delta_{g,1}\big[P_t(1,r-1) - P_t(1,r)\big] \\
   &+ \kdeg\big[(r+1)P_t(g,r+1) - r\,P_t(g,r)\big],
\end{aligned}
\end{equation}
with the convention $P_t(g,-1)=0$.
Equation~\eqref{eq:master} is the basis for all three approaches below: the SSA samples trajectories whose distribution solves it, the moment ODEs are derived from it exactly,
and the steady-state sampler draws from its $t\to\infty$ fixed point.

\section{Methods}
\label{sec:methods}

\subsection{Stochastic Simulation (Gillespie SSA)}
\label{sec:ssa}

The \emph{stochastic simulation algorithm} (SSA) of
Gillespie~\cite{gillespie1976,gillespie1977,gillespie2007} generates statistically
exact sample paths of a CTMC, making no approximation to the master
equation~\eqref{eq:master}. Its key idea is that, given the current state, two
questions separate: when does the next reaction occur, and which reaction is it?
With $a_0=\sum_{j=1}^{4} a_j$ the \emph{total propensity}, the waiting time is
$\tau\sim\mathrm{Exp}(a_0)$ and reaction $j$ fires with probability $a_j/a_0$.

Our implementation draws these by inversion, using two independent uniforms
$r_1,r_2\sim\mathrm{U}(0,1)$ per step from a seeded \texttt{numpy.random} generator~\cite{harris2020},
one seed per experiment:
\begin{equation}
  \tau = \frac{1}{a_0}\,\ln\!\frac{1}{r_1},
  \qquad
  j = \min\Big\{ k : \textstyle\sum_{i=1}^{k} a_i > r_2\,a_0 \Big\}.
\end{equation}
The time is then advanced by $\tau$, the state updated by reaction $j$, the reaction
propensities recomputed, and the loop repeats for a fixed number of events
$n_{\mathrm{sim}}$. If the total propensity reaches $a_0=0$, no reaction can
fire, so the state is absorbing and is held fixed afterwards.

A single trajectory is the history of one cell. To obtain population statistics, we run $N_{\mathrm{rep}}$ independent trajectories with identical parameters and
average over them. Each trajectory advances by its own random time increments, so the same event index occurs at different times in different trajectories.
We therefore resample every trajectory onto a shared uniform time grid by a zero-order hold before averaging;
the state is piecewise constant between events, so this introduces no error. The empirical moments are then ensemble averages at each grid time, for example
\begin{equation}
\label{eq:ssa-mean}
  \widehat{\muR}(t)
  = \frac{1}{N_{\mathrm{rep}}}\sum_{i=1}^{N_{\mathrm{rep}}} R_i(t),
  \qquad
  \widehat{\muG}(t)
  = \frac{1}{N_{\mathrm{rep}}}\sum_{i=1}^{N_{\mathrm{rep}}} G_i(t),
\end{equation}
with sample variances and the sample gene--RNA covariance defined analogously.

\paragraph{Simulation horizon.}
The loop counter $n_{\mathrm{sim}}$ bounds the number of \emph{events}, not the elapsed time, so an
ensemble has no common end time. We take the shared grid no further than the
earliest-finishing trajectory, $t_{\mathrm{end}}=\min_i t_i^{\,\text{last}}$; extending past that point
would freeze the exhausted trajectories at their final state and bias the ensemble toward the states
those cells held when their event budget ran out. Each figure ends at its own seed-dependent time, in
every case a large multiple of the relaxation time.

A typical sample path is shown in Figure~\ref{fig:single-cell}. The mRNA count climbs in a burst while
the gene is ON and decays roughly exponentially once it switches OFF, which is the mechanism behind
over-dispersion; and the decay is slow enough that the count is still well above zero when the next ON
period begins, so a cell can be found with $G=0$ and $R>0$. The steady-state sampler of
Section~\ref{sec:ss} reproduces this property.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.85\textwidth]{single_cell_trajectory.png}
  \caption{A single SSA sample path at the reference parameters of
    Section~\ref{sec:results}. Top: the gene state switches
    between OFF and ON at random times. Bottom: the mRNA count rises in bursts during ON periods and
    decays between them. Averaging many such trajectories according to~\eqref{eq:ssa-mean} gives the
    ensemble moments compared with the moment ODEs in Section~\ref{sec:res-ssa-ode}.}
  \label{fig:single-cell}
\end{figure}

\subsection{Moment ODEs}
\label{sec:ode}

The SSA describes individual cells. The population-level statistics---the \emph{moments}---obey
deterministic equations instead.
A moment is an expectation: the mean $\muR=\E[R]$ is the average mRNA level, the variance
$\sigRsq=\E[R^2]-\E[R]^2$ measures the noise, and the covariance
$\CRG=\E[RG]-\E[R]\E[G]$ measures how gene activity and mRNA level vary together.

Applying the standard moment rule for a chemical master equation---the expected rate of
change of any observable is the sum over reactions of each propensity times the change it
makes---to $f=G,R,R^2,GR$ in turn yields four coupled equations for the telegraph
model~\cite{ingalls}:
\begin{align}
\label{eq:dmuG}
  \frac{\mathrm{d}\muG}{\mathrm{d}t} &= \kon\,(1-\muG) - \koff\,\muG , \\
\label{eq:dmuR}
  \frac{\mathrm{d}\muR}{\mathrm{d}t} &= \ksyn\,\muG - \kdeg\,\muR , \\
\label{eq:dsigR}
  \frac{\mathrm{d}\sigRsq}{\mathrm{d}t} &= 2\ksyn\,\CRG - 2\kdeg\,\sigRsq + \ksyn\,\muG + \kdeg\,\muR , \\
\label{eq:dCRG}
  \frac{\mathrm{d}\CRG}{\mathrm{d}t} &= \ksyn\,\sigGsq - (\kon+\koff+\kdeg)\,\CRG .
\end{align}

\paragraph{Exact closure.}
In a general nonlinear reaction system the equation for each moment depends on higher moments, so
the system never closes and one has to add approximate closure schemes~\cite{schnoerr2015,wilkinson}.
Here it closes \emph{exactly}. The only possible nonlinearity is the variance of the gene state,
but because $G$ is binary,
\begin{equation}
\label{eq:closure}
  \sigGsq = \E[G^2]-\muG^2 = \E[G]-\muG^2 = \muG\,(1-\muG),
\end{equation}
which depends on the mean $\muG$ alone. Putting~\eqref{eq:closure} into~\eqref{eq:dCRG} turns the
four equations \eqref{eq:dmuG}--\eqref{eq:dCRG} into a linear system in
$(\muG,\muR,\sigRsq,\CRG)$ whose solutions are the true moments of the master
equation~\eqref{eq:master}.

\paragraph{Implementation.}
The four right-hand sides
\eqref{eq:dmuG}--\eqref{eq:dCRG}, with $\sigGsq$ substituted by~\eqref{eq:closure}, are coded
directly and passed to the explicit Runge--Kutta solver \texttt{scipy.integrate.solve\_ivp}~\cite{virtanen2020}
(method \texttt{RK45}, the embedded Dormand--Prince pair~\cite{dormand1980}) from the initial conditions $\muG(t_0)=g_0$, $\muR(t_0)=r_0$,
$\sigRsq(t_0)=\CRG(t_0)=0$. Section~\ref{sec:res-ssa-ode} overlays the result on the SSA
ensemble at the same rates and the same initial condition.

\paragraph{Steady-state Fano factor.}
The Fano factor at stationarity follows from the same four equations in three steps.

First, set the right-hand side of~\eqref{eq:dCRG} to zero and use the closure~\eqref{eq:closure}
for $\sigGsq$:
\begin{equation}
\label{eq:crg-ss}
  \CRG^{ss} = \frac{\ksyn\,\sigGsq}{\kon+\koff+\kdeg}
            = \frac{\ksyn\,\muG^{ss}(1-\muG^{ss})}{\kon+\koff+\kdeg} .
\end{equation}
Second, set the right-hand side of~\eqref{eq:dsigR} to zero and solve for the variance:
\begin{equation}
\label{eq:sigR-ss}
  2\kdeg\,\sigRsq = 2\ksyn\,\CRG^{ss} + \ksyn\,\muG^{ss} + \kdeg\,\muR^{ss} .
\end{equation}
Third, note that $\ksyn\muG^{ss}=\kdeg\muR^{ss}$ by~\eqref{eq:dmuR}, so the last two terms
of~\eqref{eq:sigR-ss} are equal. Substituting~\eqref{eq:crg-ss} and dividing through by
$\muR^{ss}$ gives the Fano factor in closed form:
\begin{equation}
\label{eq:fano}
  F^{ss} \;=\; \frac{\sigRsq}{\muR}\bigg|_{ss}
  \;=\; 1 \;+\; \frac{\ksyn\,\koff}{(\kon+\koff)\,(\kon+\koff+\kdeg)} .
\end{equation}
The additive $1$ is the Poisson baseline. The second term is the excess contributed by promoter
switching. It is strictly positive whenever $\koff>0$, decays like $1/(\kon+\koff)$ at fixed ratio,
and vanishes in two limits: as $\koff\to0$, where the gene is always ON and the mRNA count becomes a
pure birth--death process, and as $\kon,\koff\to\infty$, where a promoter that switches very fast
averages out.

Setting the time derivatives in~\eqref{eq:dmuG}--\eqref{eq:dmuR} to zero in the same way gives the exact
stationary means used as the reference throughout Section~\ref{sec:results}:
\begin{equation}
\label{eq:ss-means}
  \muG^{ss} = \frac{\kon}{\kon+\koff},
  \qquad
  \muR^{ss} = \frac{\ksyn}{\kdeg}\,\muG^{ss}.
\end{equation}

\subsection{Steady-State Sampler}
\label{sec:ss}

As $t\to\infty$ the telegraph model forgets its initial condition and converges to a unique
stationary distribution $P_\infty(g,r)$. Instead of simulating a long transient to reach it, we can
sample from $P_\infty$ directly. Peccoud and Ycart~\cite{peccoud1995} showed that at steady state
the mRNA count follows the classic \emph{Beta--Poisson} law: it is Poisson, but with a mean that is
itself random from cell to cell. Let the \emph{mixing fraction} $p\in[0,1]$ be the long-run share of
time a cell's gene spends ON. Across a population $p$ is Beta-distributed, and given $p$ the mRNA
count is Poisson with mean $\lambda=c\,p$, where $c=\ksyn/\kdeg$ is the \emph{transcription
efficiency coefficient}: the mean mRNA level a permanently active gene would reach. With the
switching rates $a=\kon/\kdeg$ and $b=\koff/\kdeg$ (so $\kdeg$ fixes the unit of time),
the stationary mRNA law is therefore
\begin{equation}
\label{eq:beta-poisson}
  R \sim \mathrm{Poisson}(c\,p), \qquad p \sim \mathrm{Beta}(a,b).
\end{equation}

Averaging the Poisson kernel over the Beta density gives $P_\infty(r)$ in closed form, as a confluent
hypergeometric expression~\cite{peccoud1995}. Because the mixture~\eqref{eq:beta-poisson} \emph{is}
that distribution, sampling the mixture samples $P_\infty$ exactly, using three draws per sample and
no discretisation of the state space.

Our sampler draws the gene state as an explicit
$\mathrm{Bernoulli}(p)$ so we can also estimate the joint moments of $(G,R)$. For each of the
$N_{\mathrm{rep}}$ samples it draws
\begin{align}
  p &\sim \mathrm{Beta}(a,b),
  &&f(p) = \frac{\Gamma(a+b)}{\Gamma(a)\,\Gamma(b)}\,p^{\,a-1}(1-p)^{\,b-1}, \\[2pt]
  G \mid p &\sim \mathrm{Bernoulli}(p),
  &&\Pr(G=1\mid p)=p,\quad \Pr(G=0\mid p)=1-p, \\[2pt]
  R \mid p &\sim \mathrm{Poisson}(\lambda),
  &&\lambda = \frac{\ksyn}{\kdeg}\,p .
\end{align}
The mRNA count $R$ is conditioned on the mixing fraction $p$, \emph{not} on
the current gene state $G$. Given $p$, the gene state and the mRNA count are drawn independently, so
a cell can have $G=0$ and still $R>0$---as in a real cell, where mRNA made during the last active
period survives past the switch to OFF.

\paragraph{The joint construction.}
Peccoud and Ycart~\cite{peccoud1995} derived the \emph{marginal} law of $R$, whereas the checks below need
$\widehat{\sigma}_G^{\,ss}$ and $\widehat{\CRG}^{\,ss}$, which are properties of the joint law. Coupling
$G$ to the same mixing fraction $p$ that drives the Poisson mean supplies that joint law, and it
reproduces the covariance $\CRG^{ss}$ of~\eqref{eq:crg-ss}, obtained independently in
Section~\ref{sec:ode} by setting~\eqref{eq:dCRG} to zero.

Because $G$ and $R$ are conditionally
independent given $p$,
\begin{equation}
\label{eq:erg}
  \E[RG] = \E\big[\E[R\mid p]\,\E[G\mid p]\big] = c\,\E[p^2],
  \qquad \E[R]\,\E[G] = c\,\E[p]^2 ,
\end{equation}
so that $\CRG = c\,\mathrm{Var}(p)$. Substituting the Beta variance and rewriting each factor in
terms of the original rates then gives
\begin{align}
\label{eq:crg-smp}
  \CRG
  &= c\cdot\frac{ab}{(a+b)^2\,(a+b+1)}
   = \frac{\ksyn}{\kdeg}\cdot\frac{\muG^{ss}(1-\muG^{ss})}{(\kon+\koff+\kdeg)/\kdeg} \notag \\[2pt]
  &= \frac{\ksyn\,\muG^{ss}(1-\muG^{ss})}{\kon+\koff+\kdeg} ,
\end{align}
using $ab/(a+b)^2=\muG^{ss}(1-\muG^{ss})$, $a+b+1=(\kon+\koff+\kdeg)/\kdeg$ and $c=\ksyn/\kdeg$.
This matches~\eqref{eq:crg-ss} term for term, so the construction reproduces the joint moments of the
master equation. The match is an algebraic identity rather than an empirical result: the numerical
check in Section~\ref{sec:res-ss} tests the Monte Carlo estimator, not the construction itself.

\paragraph{Boundary behaviour.}
As the switching rates become slow compared with degradation ($a,b\to 0$), the $\mathrm{Beta}(a,b)$
density concentrates its mass at the endpoints $p=0$ and $p=1$, and the mixture separates into a single
Bernoulli trial for the gene state and, when ON, an ordinary Poisson count for the mRNA. The limiting
cases $\koff=0$ and $\kdeg=0$ are treated in Section~\ref{sec:res-ss}.

The joint stationary law is the average of the conditional distributions over the mixing fraction,
$P_\infty(g,r) = \int_0^1 \Pr(G=g\mid p)\,\Pr(R=r\mid p)\,f(p)\,\mathrm{d}p$; drawing
$N_{\mathrm{rep}}$ triples $(p,G,R)$ and averaging gives unbiased Monte Carlo estimates of the
steady-state moments $\widehat{\muG}^{\,ss},\widehat{\muR}^{\,ss}$, and so on.

\section{Experiments and Results}
\label{sec:results}

Unless stated otherwise, the experiments use the symmetric \emph{reference parameters}
$\kon=0.5$, $\koff=0.5$, $\ksyn=20.0$, $\kdeg=1.0$, with $N_{\mathrm{rep}}=1000$ trajectories and initial condition
$(g_0,r_0)=(0,0)$, giving $\muG^{ss}=0.5$ and $\muR^{ss}=10$ by~\eqref{eq:ss-means}.
The parameter-exploration runs of Section~\ref{sec:res-explore} are the one exception: they
vary rates around a slower base of $\kon=\koff=0.1$, stated with each run in
Table~\ref{tab:param-exp}.
All three parts of the validation are implemented in Python; see the repository linked above.

\subsection{The Validation Triangle}
\label{sec:intersection}

The three descriptions of Section~\ref{sec:methods} must agree wherever their domains overlap. We state
those overlaps as the three edges of a \emph{validation triangle} and test each one numerically below.

\emph{Edge 1} (SSA $\leftrightarrow$ moment ODEs, finite time): the empirical
averages~\eqref{eq:ssa-mean} are sample means over i.i.d.\ trajectories, so they converge to the
expectations $\muG(t),\muR(t),\CRG(t)$ solving~\eqref{eq:dmuG}--\eqref{eq:dCRG}, and at finite
$N_{\mathrm{rep}}$ must follow the ODE curves up to an error of order $N_{\mathrm{rep}}^{-1/2}$.
\emph{Edge 2} (moment ODEs $\leftrightarrow$ steady-state sampler, long time): the ODEs relax to the
fixed point~\eqref{eq:ss-means}, which the sampler must estimate at the same $N_{\mathrm{rep}}^{-1/2}$
rate. \emph{Edge 3} (SSA $\leftrightarrow$ steady-state sampler): a trajectory run to stationarity
samples the same $P_\infty$ as the sampler. Since both endpoints of this edge produce samples, Edge~3
can be tested as equality of the full laws, $\hat P^{\mathrm{SSA}}_\infty=\hat P^{\mathrm{smp}}_\infty$,
rather than of their first two moments alone; Section~\ref{sec:res-ss} does so in total-variation
distance.

\subsection{Parameter Exploration}
\label{sec:res-explore}

Before testing the three edges, we verify that the SSA accurately reproduces the closed-form Fano factor~\eqref{eq:fano} across different parameter regimes. Table~\ref{tab:param-exp} isolates the effects of varying biological parameters and numerical settings.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.49\textwidth]{param_switching_slow.png}
  \hfill
  \includegraphics[width=0.49\textwidth]{param_switching_fast.png}
  \caption{Switching speed reshapes noise at a fixed mean ($\muR\approx10$). Slow switching (left,
    $\kon=\koff=0.05$) produces long bursts, a wide $\pm\sigma_R$ band, and strong
    gene--RNA covariance. Fast switching (right, $\kon=\koff=10$) averages the
    promoter out, leaving a narrow band and near-zero covariance.}
  \label{fig:wf-switch}
\end{figure}

As expected, switching speed modulates noise at a fixed mean (Figure~\ref{fig:wf-switch}): faster switching averages out the gene state, dropping $\sigRsq$ and $\CRG$ and driving the Fano factor toward the Poisson baseline. Conversely, varying the burst size~\eqref{eq:burst} alters the steady-state mean $\muR^{ss}$. The expression runs demonstrate that a weaker promoter yields fewer molecules but a larger relative spread: the coefficient of variation $\sigma_R/\muR$ rises from $1.268\pm0.012$ in the high-expression run to $1.561\pm0.006$ in the low-expression run (Figure~\ref{fig:expr-low}). 

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.6\textwidth]{param_expression_low.png}
  \caption{The low-expression regime ($\kon=0.5$, $\koff=10$, $\ksyn=30$), a weak promoter with
    burst size $B\approx3$. The mean mRNA level is low and the relative spread correspondingly
    large; panel layout follows Figure~\ref{fig:wf-switch}.}
  \label{fig:expr-low}
\end{figure}

The steady state is robust to both initial conditions and numerical resolution. Across all nine runs in Table~\ref{tab:param-exp}, the measured Fano factors agree with the analytical steady state~\eqref{eq:fano} within $1.2$ Monte Carlo standard errors (maximum relative deviation of $1.6\%$). The numerical experiments also confirm expected Monte Carlo error scaling: reducing the ensemble size $N_{\mathrm{rep}}$ from $1000$ to $224$ leaves $\widehat{F}$ unbiased while widening its standard error predictably.

\begin{table}[htbp]
  \centering
  \small
  \begin{tabular}{llcl}
  \toprule
  \textbf{Experiment} & \textbf{Parameters} & $F^{ss}$ & $\widehat{F}$ (SSA) \\
  \midrule
  \multicolumn{4}{l}{\emph{Noise at a fixed mean}} \\
  Slow switching   & $\kon=\koff=0.05$, $\ksyn=20$   & 10.09 & $9.94\pm0.24$ \\
  Fast switching   & $\kon=\koff=10$, $\ksyn=20$     & 1.48  & $1.48\pm0.02$ \\
  \midrule
  \multicolumn{4}{l}{\emph{Burst size}} \\
  High expression  & $\kon=0.5,\koff=4,\ksyn=56$     & 10.05 & $9.90\pm0.13$ \\
  Low expression   & $\kon=0.5,\koff=10,\ksyn=30$    & 3.48  & $3.49\pm0.03$ \\
  \midrule
  \multicolumn{4}{l}{\emph{Forgetting the initial condition}} \\
  Base, from $(0,0)$   & $(g_0,r_0)=(0,0)$           & 9.33  & $9.41\pm0.16$ \\
  From $(1,15)$        & $(g_0,r_0)=(1,15)$          & 9.33  & $9.27\pm0.08$ \\
  \midrule
  \multicolumn{4}{l}{\emph{Numerical settings}} \\
  Fewer events     & $n_{\mathrm{sim}}=1000$         & 9.33  & $9.48\pm0.21$ \\
  Smaller ensemble & $N_{\mathrm{rep}}=224$          & 9.33  & $9.34\pm0.46$ \\
  Both             & $N_{\mathrm{rep}}=224,\,n_{\mathrm{sim}}=5000$ & 9.33 & $9.40\pm0.18$ \\
  \bottomrule
  \end{tabular}
  \caption{The parameter-exploration runs. Unless stated otherwise, $\kdeg=1$, base
    switching rates are $\kon=\koff=0.1$, $N_{\mathrm{rep}}=1000$, and $n_{\mathrm{sim}}=4000$. Each
    run maintains the reference steady-state mean except the expression runs. $F^{ss}$ is the analytical Fano factor~\eqref{eq:fano}; $\widehat{F}=\widehat{\sigma}_R^2/\widehat{\muR}$ is the SSA estimate averaged over the final fifth of the simulation. The quoted $\pm$ represents the standard deviation across $10$ independent seeds (the Monte Carlo error of a single run).}
  \label{tab:param-exp}
\end{table}
\subsection{SSA vs.\ ODE: Moment Dynamics (Edge 1)}
\label{sec:res-ssa-ode}

The direct test of Edge~1 is the overlay in Figure~\ref{fig:ssa_vs_ode}, where the SSA ensemble averages follow the deterministic ODE curves throughout the transient relaxation and into the steady state. The visible deviations are confined to the covariance curve---the noisiest of the three statistics---and shrink as $N_{\mathrm{rep}}$ increases. The $\pm\sigma$ bands are wide because they represent true cell-to-cell variability rather than numerical error, perfectly matching the analytical spread predicted by the moment ODEs.

This agreement extends beyond the symmetric reference case: the SSA accurately tracks the deterministic dynamics under slow and fast switching, high and low expression, and various initial conditions. For instance, Figure~\ref{fig:wf-ssaode} demonstrates that the exact tracking is preserved in the high-noise slow-switching regime.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.9\textwidth]{ssa_vs_ode.png}
  \caption{SSA ensemble averages (green, with empirical $\pm\sigma$ fluctuation bands shaded)
    and the deterministic ODE solutions (dashed blue) at the reference parameters. In the mRNA panel, the analytical spread from
    the moment ODEs, $\muR\pm\sigma_R$, is drawn as amber dotted lines on top of the SSA band.}
  \label{fig:ssa_vs_ode}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.85\textwidth]{ssaode_slow.png}
  \caption{SSA versus moment ODEs in the high-noise slow-switching regime; formatting follows
    Figure~\ref{fig:ssa_vs_ode}. Edge~1 holds beyond the symmetric reference case.}
  \label{fig:wf-ssaode}
\end{figure}

While the overlays demonstrate qualitative agreement, Figure~\ref{fig:edge1_convergence} rigorously quantifies the convergence rate, which Edge~1 predicts to be $\mathcal{O}(1/\sqrt{N_{\mathrm{rep}}})$. We measure the root-mean-square (RMS) deviation across all grid points to ensure a robust, low-variance estimator. On log--log axes, the deviation is highly linear over three decades. A fit over this stable range yields a slope of $-0.509\pm0.010$, seamlessly matching the $-1/2$ scaling predicted by the Law of Large Numbers.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{edge1_convergence.png}
  \caption{Convergence of the SSA ensemble mean to the moment-ODE solution (Edge~1) at the reference
    parameters. The statistic is the RMS deviation of $\widehat{\muR}(t)$ from the ODE curve over the
    full time grid; the dashed line is a $1/\sqrt{N_{\mathrm{rep}}}$ reference anchored at the
    smallest ensemble. This is the Edge~1 counterpart of Figure~\ref{fig:ss_convergence}, which tests Edge~2.}
  \label{fig:edge1_convergence}
\end{figure}

Edge~1 therefore holds across every regime tested. What converges is the estimate of the mean: the
$\pm\sigma$ bands measure persistent cell-to-cell variability, described by the variance
equation~\eqref{eq:dsigR}, and are independent of $N_{\mathrm{rep}}$.

\subsection{Steady-State Validation (Edges 2 \& 3)}
\label{sec:res-ss}

Edges~2 and~3 of Section~\ref{sec:intersection} concern the stationary regime. This section tests both,
and checks that all three descriptions reduce correctly to the known limits at the boundaries of
parameter space.

The ODE solutions in Figure~\ref{fig:ssa_vs_ode} plateau at the analytical fixed points~\eqref{eq:ss-means},
and the steady-state sampler reproduces the same values to within Monte Carlo error.
Figure~\ref{fig:ss_convergence} makes the convergence quantitative: on log--log axes the mean absolute
deviation of the sampler's $\muR^{ss}$ estimate has a fitted slope of $-0.505\pm0.008$ over four
decades, against the $-1/2$ of the Central Limit Theorem. Unlike the Edge-1 fit, all $25$ points here
average over the same number of trials, so the fit needs no reweighting.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.8\textwidth]{steady_state_convergence.png}
  \caption{Convergence of the steady-state sampler's empirical moments to
    the analytical steady-state values as a function of the ensemble size
    $N_{\mathrm{rep}}$. The error (mean absolute deviation) decays as
    $1/\sqrt{N_{\mathrm{rep}}}$, the rate given by the Central Limit Theorem.}
  \label{fig:ss_convergence}
\end{figure}

A stricter check asks the sampler to reproduce \emph{all five} steady-state moments at once.
Table~\ref{tab:moments} compares the sampled moments against the closed forms~\eqref{eq:crg-ss}
and~\eqref{eq:ss-means} in an asymmetric regime. All five agree to within a few Monte Carlo standard
errors.

The five rows are not five independent confirmations. All five moments are computed from a single
array of Beta draws, so their deviations are strongly correlated.

\begin{table}[htbp]
  \centering
  \begin{tabular}{lcccc}
  \toprule
  \textbf{Moment} & \textbf{Analytic} & \textbf{Sampler} & \textbf{Abs.\ diff.} & \textbf{Std.\ error} \\
  \midrule
  $\muG^{ss}$    & 0.3333 & 0.3317 & 0.0016 & 0.0010 \\
  $\muR^{ss}$    & 6.6667 & 6.6277 & 0.0389 & 0.0142 \\
  $\sigma_G^{ss}$ & 0.4714 & 0.4708 & 0.0006 & 0.0004 \\
  $\sigma_R^{ss}$ & 6.4979 & 6.4767 & 0.0212 & 0.0105 \\
  $\CRG^{ss}$    & 1.7778 & 1.7666 & 0.0111 & 0.0071 \\
  \bottomrule
  \end{tabular}
  \caption{Steady-state moments from the sampler ($N_{\mathrm{rep}}=2\times10^5$)
    against exact analytical values for $\kon=0.5,\koff=1,\ksyn=20,\kdeg=1$.
    The final column is the Monte Carlo standard error, obtained by bootstrapping over
    $400$ resamples.}
  \label{tab:moments}
\end{table}

The boundary limits predicted in Section~\ref{sec:ss} also hold. At $\koff=0$ the model reduces to a
birth--death process with a Poisson steady state---setting $\koff=0$ in~\eqref{eq:fano} kills the excess
term and leaves $F^{ss}=1$ exactly: the moment ODEs relax to
$\muG^{ss}=1$ and $\muR^{ss}=\ksyn/\kdeg$, and the sampled counts overlay the analytic Poisson PMF
(Figure~\ref{fig:wf-poisson}) with a measured Fano factor of $0.9953\pm0.0043$ against the
theoretical $1$. At the opposite boundary, $\kdeg=0$, mRNA accumulates without bound and no steady state
exists.

\paragraph{Edge 3 as a distributional test.}
Since both endpoints of Edge~3 produce samples, the full stationary laws can be compared directly, using the Total Variation distance
$\mathrm{TV}=\tfrac12\sum_r|\hat P^{\mathrm{SSA}}(r)-\hat P^{\mathrm{smp}}(r)|$. On the SSA side the mRNA count is recorded at one late time point per
trajectory, which costs samples but avoids the autocorrelation that would understate the spread.

The null distribution for TV is obtained by \emph{direct resimulation}: assuming both methods sample the same
stationary law, we draw 200 fresh pairs at the same sample sizes and recompute the statistic. We avoid
bootstrapping each side from its own empirical measure, which would add sampling noise twice and centre the
null band above the true expected TV, making the test too lenient.

The two regimes in Figure~\ref{fig:edge3} put very different demands on the test. While the symmetric reference regime (left) presents a relatively standard distribution,
 the slow-switching regime (right) is highly bimodal. 
A method could match the basic moments $\muR$ and $\sigma_R$ exactly and still miss this complex shape, so achieving agreement here rigorously compares the tw
o implementations where the distribution is furthest from anything the moments describe.

Measured by total variation (TV) distance, the two laws strongly agree. In the slow-switching regime, 
$\mathrm{TV}=0.0117$ ($p=0.74$), and in the symmetric regime, $\mathrm{TV}=0.0093$ ($p=1.00$). Neither value exceeds what sampling variation alone would produce. 
Consequently, no statistically significant discrepancy between the two laws is detected in either regime.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\textwidth]{edge3_distribution.png}
  \caption{Edge~3 as a distributional test: the stationary mRNA distribution from $20\,000$
    independent SSA trajectories (green bars, one sample per trajectory) against
    $2\times10^5$ draws from the steady-state sampler (dashed blue). Left: symmetric reference
    regime. Right: slow switching ($\kon=\koff=0.05$), where the distribution is bimodal.}
  \label{fig:edge3}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.7\textwidth]{validation_poisson.png}
  \caption{Boundary case $\koff=0$ (permanently active gene): the sampled steady-state mRNA
    distribution against the analytic Poisson PMF with mean $\ksyn/\kdeg$.}
  \label{fig:wf-poisson}
\end{figure}

These checks show the sampler to be an unbiased, $\sqrt{N}$-consistent estimator of the steady-state
moments, so Edge~2 and Edge~3 hold.
\FloatBarrier
\section{Conclusion}
\label{sec:conclusion}

All three edges of the validation triangle hold. Their value lies in what the three implementations do
not share: a trajectory simulator, a deterministic ODE integrator, and a Beta--Poisson sampler have no
common code and fail in different ways, so an error in the SSA event loop, a mis-derived moment equation,
or a wrong stationary law would each disturb one edge while leaving the others intact. The reductions to
classical limits at $\koff=0$ and $\kdeg=0$ anchor this at the two boundaries where the answer is known
in advance.

The triangle validates the three implementations against one another over the regimes tested; 

\section{Future Work}
\label{sec:future}
With the telegraph model established as a validated baseline, two directions follow: one
that strengthens the validation, and one that captures more of the underlying biology.

\paragraph{Gene regulatory networks}
In vivo, gene products often toggle other genes, forming regulatory
networks that can be described by mapping the interacting genes to an Ising model, where ON/OFF
states act as spins and regulatory links as couplings. The obstacle is the size of the state space: for
n genes the joint distribution spans 2n promoter congurations, each multiplying the unbounded
mRNA and protein spaces, so direct integration gives way to approximate high-dimensional density
estimators.

\paragraph{Incorporating the protein level.}
A three-stage model (gene $\to$ mRNA $\to$ protein) would add translation at rate $k_{\mathrm{trans}}$ and protein degradation at rate $k_{\mathrm{deg},p}$.
The tools validated here carry over directly: the SSA handles the two additional reactions unchanged, and the moment ODE system expands to cover protein variance and mRNA--protein covariance.

\begin{thebibliography}{99}

\bibitem{gillespie1976}
D.~T.~Gillespie,
\textit{A general method for numerically simulating the stochastic time evolution
of coupled chemical reactions},
Journal of Computational Physics \textbf{22} (1976), no.~4, 403--434.

\bibitem{gillespie1977}
D.~T.~Gillespie,
\textit{Exact stochastic simulation of coupled chemical reactions},
Journal of Physical Chemistry \textbf{81} (1977), no.~25, 2340--2361.

\bibitem{gillespie2007}
D.~T.~Gillespie,
\textit{Stochastic simulation of chemical kinetics},
Annual Review of Physical Chemistry \textbf{58} (2007), no.~1, 35--55.

\bibitem{peccoud1995}
J.~Peccoud and B.~Ycart,
\textit{Markovian modeling of gene-product synthesis},
Theoretical Population Biology \textbf{48} (1995), no.~2, 222--234.

\bibitem{raj2006}
A.~Raj, C.~S.~Peskin, D.~Tranchina, D.~Y.~Vargas, and S.~Tyagi,
\textit{Stochastic mRNA synthesis in mammalian cells},
PLoS Biology \textbf{4} (2006), no.~10, e309.

\bibitem{elowitz2002}
M.~B.~Elowitz, A.~J.~Levine, E.~D.~Siggia, and P.~S.~Swain,
\textit{Stochastic gene expression in a single cell},
Science \textbf{297} (2002), no.~5584, 1183--1186.

\bibitem{ozbudak2002}
E.~M.~Ozbudak, M.~Thattai, I.~Kurtser, A.~D.~Grossman, and A.~van Oudenaarden,
\textit{Regulation of noise in the expression of a single gene},
Nature Genetics \textbf{31} (2002), no.~1, 69--73.

\bibitem{ko1991}
M.~S.~H.~Ko,
\textit{A stochastic model for gene induction},
Journal of Theoretical Biology \textbf{153} (1991), no.~2, 181--194.

\bibitem{fano1947}
U.~Fano,
\textit{Ionization yield of radiations. II. The fluctuations of the number of ions},
Physical Review \textbf{72} (1947), no.~1, 26--29.

\bibitem{golding2005}
I.~Golding, J.~Paulsson, S.~M.~Zawilski, and E.~C.~Cox,
\textit{Real-time kinetics of gene activity in individual bacteria},
Cell \textbf{123} (2005), no.~6, 1025--1036.

\bibitem{iyerbiswas2009}
S.~Iyer-Biswas, F.~Hayot, and C.~Jayaprakash,
\textit{Stochasticity of gene products from transcriptional pulsing},
Physical Review E \textbf{79} (2009), no.~3, 031911.

\bibitem{schnoerr2015}
D.~Schnoerr, G.~Sanguinetti, and R.~Grima,
\textit{Comparison of different moment-closure approximations for stochastic
chemical kinetics},
Journal of Chemical Physics \textbf{143} (2015), no.~18, 185101.

\bibitem{munsky2006}
B.~Munsky and M.~Khammash,
\textit{The finite state projection algorithm for the solution of the chemical
master equation},
Journal of Chemical Physics \textbf{124} (2006), no.~4, 044104.

\bibitem{vankampen}
N.~G.~van Kampen,
\textit{Stochastic Processes in Physics and Chemistry},
3rd ed., North-Holland, Amsterdam, 2007.

\bibitem{wilkinson}
D.~J.~Wilkinson,
\textit{Stochastic Modelling for Systems Biology},
2nd ed., Chapman \& Hall/CRC Mathematical and Computational Biology Series, Boca Raton, 2011.

\bibitem{ingalls}
B.~P.~Ingalls,
\textit{Mathematical Modelling in Systems Biology: An Introduction},
University of Waterloo, 2012.

\bibitem{harris2020}
C.~R.~Harris et al.,
\textit{Array programming with NumPy},
Nature \textbf{585} (2020), no.~7825, 357--362.

\bibitem{virtanen2020}
P.~Virtanen et al.,
\textit{SciPy 1.0: fundamental algorithms for scientific computing in Python},
Nature Methods \textbf{17} (2020), no.~3, 261--272.

\bibitem{dormand1980}
J.~R.~Dormand and P.~J.~Prince,
\textit{A family of embedded Runge--Kutta formulae},
Journal of Computational and Applied Mathematics \textbf{6} (1980), no.~1, 19--26.

\end{thebibliography}

\end{document}
